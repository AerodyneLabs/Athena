import {
  roundNumber
} from 'athena/helpers/round-number';

module('RoundNumberHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = roundNumber(42);
  ok(result);
});
