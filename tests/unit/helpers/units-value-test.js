import {
  unitsValue
} from 'athena/helpers/units-value';

module('UnitsValueHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = unitsValue(42);
  ok(result);
});
