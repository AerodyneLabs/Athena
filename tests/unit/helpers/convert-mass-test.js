import {
  convertMass
} from '../../../helpers/convert-mass';
import { module, test } from 'qunit';

module('ConvertMassHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = convertMass(42);
  assert.ok(result);
});
