import {
  convertTemperature
} from '../../../helpers/convert-temperature';
import { module, test } from 'qunit';

module('ConvertTemperatureHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = convertTemperature(42);
  assert.ok(result);
});
