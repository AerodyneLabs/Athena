import {
  convertLength
} from '../../../helpers/convert-length';
import { module, test } from 'qunit';

module('ConvertLengthHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = convertLength(42);
  assert.ok(result);
});
