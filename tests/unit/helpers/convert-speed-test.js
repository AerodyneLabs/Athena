import {
  convertSpeed
} from '../../../helpers/convert-speed';
import { module, test } from 'qunit';

module('ConvertSpeedHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = convertSpeed(42);
  assert.ok(result);
});
