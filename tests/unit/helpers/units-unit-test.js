import {
  unitsUnit
} from 'athena/helpers/units-unit';

module('UnitsUnitHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = unitsUnit(42);
  ok(result);
});
