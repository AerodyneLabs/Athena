import {
  unitsFull
} from 'athena/helpers/units-full';

module('UnitsFullHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = unitsFull(42);
  ok(result);
});
