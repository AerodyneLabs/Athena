import {
  convertPressure
} from '../../../helpers/convert-pressure';
import { module, test } from 'qunit';

module('ConvertPressureHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = convertPressure(42);
  assert.ok(result);
});
