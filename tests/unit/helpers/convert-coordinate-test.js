import {
  convertCoordinate
} from '../../../helpers/convert-coordinate';
import { module, test } from 'qunit';

module('ConvertCoordinateHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = convertCoordinate(42);
  assert.ok(result);
});
